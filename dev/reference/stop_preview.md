# Stop HTTP preview

Stops the HTTP server started by
[`preview_site()`](https://pkgdown.r-lib.org/dev/reference/preview_site.md),
if active. This can be called manually, but is not strictly necessary as
the server is automatically stopped when previewing a new site or ending
the R session.

## Usage

``` r
stop_preview()
```
